---
description: Primary source documents for generating CMP admin docs (not published on site)
alwaysApply: true
---

# Doc generation sources

When writing or updating files under `docs/`, use these internal sources — do **not** add source URLs to published pages unless the user asks.

## Primary Google Doc

- **Title:** Copy of CMP Admin Documentation - CHATGPT
- **Doc ID:** `1H44tP15QM6yN9D_3zieNI_GiZT4yGFTYPD00uoNfgwo`
- **URL:** https://docs.google.com/document/d/1H44tP15QM6yN9D_3zieNI_GiZT4yGFTYPD00uoNfgwo/edit
- **MCP:** Read via `google-drive` MCP (`gdrive:///1H44tP15QM6yN9D_3zieNI_GiZT4yGFTYPD00uoNfgwo` or search by title)
- **Registry:** `.cursor/sources.json`

Treat this as the authoritative source for CMP admin content. Match existing Docusaurus conventions (frontmatter, admonitions with `:::type[Title]` syntax, relative internal links).

## Secondary Google Doc — Features List

- **Title:** Features List - Documentation is Required
- **Doc ID:** `154BSmee2KfUPvHxLUKWUCBgKpHdqi6PC0elVmiVZTvA`
- **URL:** https://docs.google.com/document/d/154BSmee2KfUPvHxLUKWUCBgKpHdqi6PC0elVmiVZTvA/edit?tab=t.p71d8pm7oowt
- **MCP:** `gdrive:///154BSmee2KfUPvHxLUKWUCBgKpHdqi6PC0elVmiVZTvA`
- **Local cache:** `.cursor/sources/features-list.txt`

Use for feature-level documentation scope, VPC/network ACL behaviour, and identifying pages that still need content. Prefer the **primary** doc for procedural setup steps unless the features list has more detail.

## Payment gateways Google Doc

- **Doc ID:** `1X5CShCn7ZuccXeJpydYlQCOGXu-QTABrInrjsVVV578`
- **Tab:** Payment gateways (`tab=t.6njrsh3ezjr6`)
- **URL:** https://docs.google.com/document/d/1X5CShCn7ZuccXeJpydYlQCOGXu-QTABrInrjsVVV578/edit?tab=t.6njrsh3ezjr6
- **MCP:** `gdrive:///1X5CShCn7ZuccXeJpydYlQCOGXu-QTABrInrjsVVV578`
- **Local cache:** `.cursor/sources/payment-gateways.txt`

Prefer when expanding `docs/billing/payment-gateways/` pages with credential fields and provider-specific steps.

## Form and field documentation

For CMP forms (zones, templates, packages, quota requests, and similar):

- Document each field with a **bold field name** on its own line — do **not** use `##` or `###` headings for individual form fields.
- Follow the bold name with `*Required.*` / `*Optional.*` / `*Read-only.*` and full guidance inline (same pattern as package forms).
- Do **not** add a summary table that only links to sections below (no "See [Field](#anchor) below" index tables).
- Tables are still fine for **value/behaviour** within a field block (e.g. Active vs Inactive, Yes vs No, or example values).
- Screenshot placeholders: put the target path as plain text (`img/screenshots/filename.png`), then use `![…](/img/screenshots/placeholder.png)` until the real file exists.

## Wording to avoid

- Do **not** instruct admins to "raise a support ticket with StackConsole" for template updates. Use **re-configure manually** in CMP (`Settings → Orchestrator → Templates`). CMP has no self-service **Sync** option for templates.

## Read.ai

Use the `read-ai` MCP server for meeting transcripts, summaries, and action items when the user references a meeting or recent discussion.

## If MCP is unavailable

Ask the user to paste the relevant section, or export the Google Doc section as text.
